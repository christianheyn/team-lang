import (fa fb fc) from "./FeatureA.team"
import as _B from "./FeatureB.team"

{() (
    if (_B.f)
    then (fb)
    else (fc)
)}

{() (
    cond
        condition then (f1)
        condition then (f2)
        condition then (f3)
        otherwise (f4)
)}

{() (
    when condition a
)}
